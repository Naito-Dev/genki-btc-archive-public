@Genki

Dev Log #003 — 2026-03-25

EN
1) Change shipped
- Restore BTC price display when verified (fix regression) (#127)
- Merge “record” + “published” metadata to reduce inconsistency (#128)
- Backfill btcsignal close from daily log (data completeness) (#122)

2) Why
- Avoid missing/blank price UI when data is valid
- Keep timestamps/flags consistent across pages & exports
- Reduce gaps in close values for archive integrity

3) Validation
- Merged PRs (last 7d): #127 #128 #122
- Main commits since 7d include the above changes

4) Guardrails
- Facts-only; no performance claims
- UI only shows price when verified/valid
- Backfill uses existing daily-log sources (no new data promises)

5) Next
- Add automated checks for metadata drift
- Expand regression coverage around price/close rendering

btcsignal.org

JA
1) 反映した変更
- 検証済みのときBTC価格表示を復活（回帰修正）（#127）
- 「record」と「published」のメタデータを統合して不整合を減らす（#128）
- daily log から btcsignal close をバックフィル（欠損を減らす）（#122）

2) なぜ
- 有効なデータなのに価格が出ない/空になるのを防ぐ
- タイムスタンプやフラグの整合性を保つ
- close値の欠損を減らしてアーカイブの完全性を上げる

3) 検証
- 過去7日マージ済PR：#127 #128 #122
- 過去7日mainコミットに反映済

4) ガードレール
- 事実のみ（実績主張はしない）
- 価格は verified/valid のときのみ表示
- バックフィルは既存のdaily logソースに限定（新規データ保証なし）

5) 次
- メタデータ差分の自動チェック追加
- 価格/close表示まわりの回帰テスト強化

btcsignal.org
Not investment advice. Record-only system.
